include ../common/_layout
  body#main.wrapper
    include _header
      | 店舗ユーザとしてログイン
    include ../common/_messageArea
    .admin
      .adminBody
        .signboard_titleArea
          .signboard_titleArea-title
            | 店舗ユーザとしてログイン
        form.signboard_body(method="POST")
          .labeledRow
            label.labeledRow-header(for="storeEmail")
              | 店舗ユーザのメールアドレス
            .labeledRow-body
              input#storeEmail(
                name="storeEmail" type="email"
                required
              )
          .submitRow
            button.btn.outerBtn(type="submit")
              | この店舗ユーザとしてログイン

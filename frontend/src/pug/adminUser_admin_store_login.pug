include _layout
  body#main.wrapper
    include _adminHeader
      | 店舗ユーザとしてログイン
    .admin
      .adminMenu
        a.btn.outerBtn(href="/admin/store/create") 店舗ユーザ作成
        a.btn.defaultBtn(href="/admin/store/delete") 店舗ユーザ削除

      .adminBody
        .signboard_titleArea
          .signboard_titleArea-title
            | 店舗ユーザとしてログイン
        form.signboard_body(method="POST")
          .labeledRow
            label.labeledRow-header(for="storeEmail")
              | 店舗ユーザのメールアドレス
            .labeledRow-body
              input#storeEmail(
                name="storeEmail" type="email"
                required
              )
          .submitRow
            button.btn.outerBtn(type="submit")
              | この店舗ユーザとしてログイン

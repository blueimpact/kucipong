include ../common/_layout
  body#main.wrapper
    include _header
      | クーポン一覧
    .store
      .store_editBtn
        a.btn.outerBtn(href="\#{renderRoute storeCouponCreateR}") クーポン追加

    | %{ case couponAndImages }
    | %{ of [] }
    |   No Coupons
    | %{ of array }
    |   %{ forall (coupon, imageUrl) <- array }
    include ../common/_couponSimple
    |   %{ endforall }
    | %{ endcase }
